<nav class="navbar navbar-expand-lg bg-dark" data-bs-theme="dark">
	<div class="container-fluid">
		{# <a class="navbar-brand" href="{{ path('home.index') }}">Accueil
			<span class="visually-hidden">(current)</span>
		</a> #}
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarColor01">
			<ul class="navbar-nav me-auto">
				{# {% if app.user %}
					<li class="nav-item">
						<a class="nav-link active" href="{{ path('ingredient.index') }}">Mes Ingrédients
							<span class="visually-hidden">(current)</span>
						</a>
					</li>
					<li class="nav-item">
						<a class="nav-link active" href="{{ path('recipe.index') }}">Mes Recette
							<span class="visually-hidden">(current)</span>
						</a>
					</li>
				{% endif %} #}
			</ul>
			<div class="d-flex">
				<ul class="navbar-nav me-auto">
					{% if app.user %}
						<li class="nav-item dropdown" style="margin-right: 5rem">
							<a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">{{ app.user.username}}</a>
							<div class="dropdown-menu">
								<a class="dropdown-item" href="{{ path('account.index') }}">Dashboard</a>
								<a class="dropdown-item" href="{{ path('user.edit') }}">Settings</a>
								{# <a class="dropdown-item" href="{{ path('user.edit', { id : app.user.id }) }}">Modifier mes informations</a>
								<a class="dropdown-item" href="{{ path('user.edit.password', { id : app.user.id }) }}">Modifier mon mot de passe</a>
								{% if 'ROLE_ADMIN' in app.user.roles %}
								<div class="dropdown-divider"></div>
								<a class="dropdown-item" href="{{ path('admin.index') }}">Administration</a>
								{% endif %} #}
								<div class="dropdown-divider"></div>
								<a class="dropdown-item" href="{{ path('security.logout') }}">Logout</a>
							</div>
						</li>
					{% else %}
						<li class="nav-item">
							<a class="nav-link active" href="{{ path('security.login') }}">Login
								<span class="visually-hidden">(current)</span>
							</a>
						</li>
						{# <li class="nav-item">
							<a class="nav-link active" href="{{ path('security.registration') }}">Inscription
								<span class="visually-hidden">(current)</span>
							</a>
						</li> #}
					{% endif %}


				</ul>
			</div>
		</div>
	</div>
</nav>